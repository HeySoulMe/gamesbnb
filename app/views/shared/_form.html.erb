<%= simple_form_for [@boardgame, @booking] do |f| %>
	<%= f.input :start_at, as: :string, input_html: { id: "start_at" } %>
	<%= f.input :end_at,as: :string, input_html: { id: "end_at" } %>
	<%= f.button :submit, "Book !", class: "btn btn-primary" %>
	<% end %>

<script>
const start = document.querySelector(#start_at);
const end = document.querySelector(#end_at);

function_calcul() {
	
}

</script>